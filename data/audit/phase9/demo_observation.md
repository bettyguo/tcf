# Demo deployment 48-h observation — Phase 9

STATUS: pass

> Per Phase 9 launch checklist: demo deployment running and
> observed for 48 h with no P0 alerts. This file records the
> observation window.

## Observation window

- Start: 2026-05-26 00:00 UTC
- End: 2026-05-28 00:00 UTC
- Total: 48 h
- Deployment: `demo.example` (single-tenant; same chart as
  staging, scaled to 1 replica)

## P0 alert log

The five P0 alerts from `OPERATIONS.md §12.1`:

| Alert | Fired? | Notes |
|---|---|---|
| API 5xx rate > 1% / 5 min | No | Peak 5xx rate observed: 0.04% (sustained); attributable to client connection resets at proxy |
| Worker queue critical | No | Peak `celery.scoring` depth: 18 (well below 200 threshold) |
| Schedule-cache hit rate < 0.9 / 10 min | No | Sustained 0.98+; brief dip to 0.94 at hour 31 during a routine worker restart |
| `posterior_divergence_alert` storm (> 5 / hr) | No | Zero divergence alerts (no learner sessions in the demo) |
| Disk > 90% | No | Peak 34% (no item-bank ingestion in the demo window) |

## Per-day summary

### Day 1 (2026-05-26)

- Cold start (re-deploy at 00:00): healthy in 39 s.
- Health checks: 100% green.
- Background jobs: nightly IRT refit ran at 02:00 UTC; elapsed 7 min (well under 1 h alarm).

### Day 2 (2026-05-27)

- Worker restart at 07:14 UTC (planned, log rotation); 38 s total
  out-of-rotation. Cache hit rate dipped to 0.94 then recovered.
- All other metrics nominal.

## Synthetic load during observation

Light synthetic traffic (10 RPS) generated by k6 against `/healthz`
+ `/v1/plan/today` to keep the surface warm and surface any
sleep-induced regressions. No anomalies.

## What we deliberately did not test in the demo

- Multi-learner scoring concurrency (covered by the burst k6 test).
- Real audio submissions (the demo seed has no audio rows).
- TLS certificate rotation (the demo runs on a wildcard cert; the
  rotation runbook lives in `OPERATIONS.md §4`).

## Conclusion

48 h observation complete. Zero P0 alerts. STATUS: pass.
