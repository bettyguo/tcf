# GPU override for ML services. Phase 5+ adds an `ml` service here for Whisper + MFA inference.
# Use:  docker compose -f infra/docker-compose.yml -f infra/docker-compose.gpu.yml up
#
# Phase 1: placeholder only.

name: tcf-accel

services:
  # Example shape for the Phase 5 `ml` service:
  # ml:
  #   image: ghcr.io/tcf-accel/ml:latest
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: ["gpu"]
  #   environment:
  #     - NVIDIA_VISIBLE_DEVICES=all
